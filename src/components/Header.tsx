type Props = {};

const Header = (props: Props) => {
  return (
    <header>
      <div className="container">
        <h1>Live Manager</h1>
      </div>
    </header>
  );
};

export default Header;
