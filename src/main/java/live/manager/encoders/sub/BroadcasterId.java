package live.manager.encoders.sub;

import live.manager.encoders.SendMessage;

public class BroadcasterId extends SendMessage {

	public BroadcasterId(String broadcasterId) {
		super(broadcasterId, null);
	}
	
}
